sudo: required

language: java

services:
  - docker

branches:
  only:
    - master
    - stable

install: true

script:
  - ./gradlew clean build

deploy:
  - provider: script
    skip_cleanup: true
    script: ./gradlew bintrayUpload 
    on:
      branch: master